extends "schema.pkl"
import "android.pkl"
import "apple.pkl"

localizable: Localizable
renderers: Listing<StringRenderer> = new Listing {
    new android.AndroidStringRenderer {}
    new apple.AppleStringRenderer {}
}

output {
    files {
        for (renderer in renderers) {
            ...renderer.render(localizable)
        }
    }
}