amends "pkl:project"

local githubUser = "BenMMcLean"

package {
  name = "pkl-strings"
  baseUri = "package://benm.cl/\(name)" 
  version = read("file:.version").text.trim()
  packageZipUrl = "https://benm.cl/\(name)/\(name)@\(version).zip"
}